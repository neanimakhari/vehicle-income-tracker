# GPS ingest sidecar — bind TCP only on private network / localhost in production.
# Do not expose TRACKING_TCP_PORT publicly.

GPS_INGEST_SECRET=change-me-ingest-secret
API_BASE_URL=http://api:3000
TRACKING_TCP_PORT=5023
TRACKING_TCP_BIND=0.0.0.0
HEALTH_PORT=9088
