DB_HOST=postgres
DB_PORT=5432
DB_USERNAME=vit_admin
DB_PASSWORD=vit_admin_password
DB_PASSWORD_FILE=/run/secrets/postgres_password
DB_DATABASE=vit_platform
DB_SSL=false
DB_DEFAULT_SCHEMA=platform
JWT_SECRET=replace-with-strong-secret
JWT_SECRET_FILE=/run/secrets/jwt_secret
JWT_EXPIRES_IN=15m
JWT_REFRESH_SECRET=replace-with-strong-refresh-secret
JWT_REFRESH_SECRET_FILE=/run/secrets/jwt_refresh_secret
JWT_REFRESH_EXPIRES_IN=7d
FORCE_MFA_FOR_ADMINS=false
BOOTSTRAP_TOKEN=replace-with-bootstrap-secret
BOOTSTRAP_TOKEN_FILE=/run/secrets/bootstrap_token
CORS_ORIGINS=http://localhost:3001,http://localhost:3002

# For production: set NEXT_PUBLIC_API_URL so client-side requests use your public API URL (e.g. https://api.yourdomain.com). Required when building for deploy.
# NEXT_PUBLIC_API_URL=

# Mailgun HTTP API (recommended): set MAILGUN_API_KEY from Mailgun Dashboard -> Sending -> API keys
MAILGUN_API_KEY=
MAILGUN_DOMAIN=sandbox0ddb6eedbca54c0eb9d8ae2987fe5427.mailgun.org
EMAIL_FROM=Mailgun Sandbox <postmaster@sandbox0ddb6eedbca54c0eb9d8ae2987fe5427.mailgun.org>
EMAIL_TEST_SECRET=vit-test-email-secret

