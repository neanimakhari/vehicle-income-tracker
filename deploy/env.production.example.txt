DB_HOST=postgres
DB_PORT=5432
DB_USERNAME=vit_admin
DB_PASSWORD_FILE=/run/secrets/postgres_password
DB_DATABASE=vit_platform
DB_SSL=false
DB_DEFAULT_SCHEMA=platform
JWT_SECRET_FILE=/run/secrets/jwt_secret
JWT_EXPIRES_IN=15m
JWT_REFRESH_SECRET_FILE=/run/secrets/jwt_refresh_secret
JWT_REFRESH_EXPIRES_IN=7d
FORCE_MFA_FOR_ADMINS=true
MAX_LOGIN_ATTEMPTS=5
LOCKOUT_MINUTES=15
BOOTSTRAP_TOKEN_FILE=/run/secrets/bootstrap_token
# Production frontend origins (no trailing slash); required for CORS
CORS_ORIGINS=https://admin.yourdomain.com,https://app.yourdomain.com

# Public API URL for browser requests (set for production builds so Next.js bakes it in)
NEXT_PUBLIC_API_URL=https://api.yourdomain.com

# Optional: override host ports (defaults: API 3010, system-admin 3011, tenant-admin 3012)
# API_PORT=3010
# SYSTEM_ADMIN_PORT=3011
# TENANT_ADMIN_PORT=3012

# Mailgun HTTP API (preferred in production)
MAILGUN_API_KEY=
MAILGUN_DOMAIN=your-domain.mailgun.org
EMAIL_FROM=Mailgun Sandbox <postmaster@your-domain.mailgun.org>
EMAIL_TEST_SECRET=

