NODE_ENV=development
PORT=3000
CORS_ORIGINS=http://localhost:3001,http://localhost:3002

DB_HOST=127.0.0.1
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=
DB_PASSWORD_FILE=
DB_DATABASE=vit_platform
DB_SSL=false
DB_DEFAULT_SCHEMA=platform

JWT_SECRET=replace-with-strong-secret
JWT_SECRET_FILE=
JWT_EXPIRES_IN=1h
JWT_REFRESH_SECRET=replace-with-strong-refresh-secret
JWT_REFRESH_SECRET_FILE=
JWT_REFRESH_EXPIRES_IN=7d
FORCE_MFA_FOR_ADMINS=false
MAX_LOGIN_ATTEMPTS=5
LOCKOUT_MINUTES=15
BOOTSTRAP_TOKEN=replace-with-bootstrap-token
BOOTSTRAP_TOKEN_FILE=
RATE_LIMIT_TTL=60
RATE_LIMIT_LIMIT=100

# Mailgun HTTP API (recommended): get API key from Mailgun Dashboard -> Sending -> API keys.
MAILGUN_API_KEY=
MAILGUN_DOMAIN=sandbox0ddb6eedbca54c0eb9d8ae2987fe5427.mailgun.org
# Optional: for EU region use https://api.eu.mailgun.net
# MAILGUN_URL=https://api.mailgun.net
EMAIL_FROM=Mailgun Sandbox <postmaster@sandbox0ddb6eedbca54c0eb9d8ae2987fe5427.mailgun.org>
EMAIL_TEST_SECRET=vit-test-email-secret

# SMTP fallback (if MAILGUN_API_KEY not set): e.g. Mailgun SMTP or other provider.
EMAIL_HOST=smtp.mailgun.org
EMAIL_PORT=587
EMAIL_USER=
EMAIL_PASSWORD=

